spring:
  datasource:
    url: jdbc:mysql://localhost:3306/smdb
    username: art
    password: art
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
    show-sql: true
